package src.hierarchicalRegions.central;

simple Central {
    
    parameters:
        
        @class(dtnsimhierarchical::Central);
        
        // contact plan for each region, one file
        string contactPlan = default("");
}